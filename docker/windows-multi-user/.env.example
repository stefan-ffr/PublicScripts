# Windows Multi-User Container Configuration
# Copy this file to .env and customize for your environment
# cp .env.example .env

##########################################################################################################################################
# NETWORK CONFIGURATION (Macvlan)
##########################################################################################################################################

# Network interface on host (find with: ip addr or ifconfig)
# Common values: eth0, ens18, enp0s3, eno1
NETWORK_INTERFACE=eth0

# Your local network subnet
NETWORK_SUBNET=192.168.1.0/24

# Your network gateway (usually your router)
NETWORK_GATEWAY=192.168.1.1

# IP range reserved for containers (must be within NETWORK_SUBNET)
# This reserves 32 IP addresses (192.168.1.128 - 192.168.1.159)
NETWORK_IP_RANGE=192.168.1.128/27

##########################################################################################################################################
# CONTAINER IP ADDRESSES
##########################################################################################################################################

# Each container gets a unique IP address on your network
# Make sure these IPs are within NETWORK_IP_RANGE and not used by other devices
IP_USER1=192.168.1.130
IP_USER2=192.168.1.131
IP_USER3=192.168.1.132

##########################################################################################################################################
# RESOURCE ALLOCATION
##########################################################################################################################################

# Virtual disk size for each container (can be increased later without data loss)
DISK_SIZE=64G

# RAM allocation per user (6G recommended, minimum 4G)
RAM_SIZE_USER1=6G
RAM_SIZE_USER2=6G
RAM_SIZE_USER3=6G

# CPU cores per user (2 recommended, can be adjusted based on host resources)
CPU_CORES_USER1=2
CPU_CORES_USER2=2
CPU_CORES_USER3=2

##########################################################################################################################################
# STORAGE CONFIGURATION
##########################################################################################################################################

# Base path for storage volumes
# Default: ./storage (relative to docker-compose.yml)
# Alternative: /var/lib/windows-containers (absolute path)
STORAGE_PATH=./storage

##########################################################################################################################################
# NOTES
##########################################################################################################################################

# Default Windows Credentials (before first boot):
#   Username: Docker
#   Password: admin
#   IMPORTANT: Change password immediately after installation!

# Connection Methods:
#   RDP: Connect to container IP on port 3389
#   Web: http://<container-ip>:8006 (for monitoring installation)

# Resource Planning:
#   3 containers with 6GB RAM each = 18GB RAM required (+ host OS overhead)
#   3 containers with 2 CPU cores each = 6 CPU cores required (+ host OS overhead)
#   3 containers with 64GB disk each = 192GB storage required
